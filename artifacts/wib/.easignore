# Backend server (not needed for mobile build)
../../artifacts/api-server

# Unused prototypes
../../artifacts/mockup-sandbox

# Build outputs
**/dist
**/.local
**/uploads

# Caches and logs
**/.git
**/*.log
**/.cache
**/.expo
